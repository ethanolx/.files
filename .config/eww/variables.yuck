(defpoll time :interval "1s"
    "date +'%H:%M'")

(defpoll date :interval "1s"
    "date +'%a, %d %m %y'")

; sliders
(defpoll head_vol :interval "1s"
  "scripts/get_headvol")

(defpoll mic_vol :interval "1s"
  "scripts/get_micvol")

; wifi iwdgtk btn
(defpoll wifi :interval "1s"
    "scripts/wifi")

; brightness slider
(defpoll brightness :interval "1s"
  "scripts/get_brightness")
